FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./

RUN npm install

COPY . .

# Expose dev server port
EXPOSE 3000

# Mount source for hot reload (set volume via docker-compose)
CMD ["npm", "run", "dev", "--", "--host"]
